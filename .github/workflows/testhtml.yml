# This is a basic workflow to help you get started with Actions

name: createTestHtml

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      
      - name: Install XMLLint
        run: sudo apt  install libxml2-utils
      - name: Test loop
        run: |
          for file in ./templates/*; do
            name=$(xmllint --xpath "/kata/name/text()" $file)
            description=$(xmllint --xpath "/kata/description/text()" $file)
            text=$(xmllint --xpath "/kata/text" $file)
            echo "$name" | tr '[:upper:]' '[:lower:]'

            echo "" > test.html
            echo "<!DOCTYPE html>" >> test.html
            echo "<html lang=\"en\">" >> test.html
            echo "  <head>" >> test.html
            echo "    <title>$name</title>" >> test.html
            echo "    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">" >> test.html
            echo "    <meta name=\"description\" content=\"$description\">" >> test.html
            echo "    <meta charset=\"UTF-8\">" >> test.html
            echo "    <link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">" >> test.html
            echo "    <link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>" >> test.html
            echo "    <link href=\"https://fonts.googleapis.com/css2?family=Kaisei+Tokumin&display=swap\" rel=\"stylesheet\">" >> test.html
            echo "    <link href=\"style.css\" type=\"text/css\" rel=\"stylesheet\">" >> test.html
            echo "    <link rel=\"shortcut icon\" href=\"favicon.ico\" type=\"image/x-icon\">" >> test.html
            echo "  </head>" >> test.html
            echo "  <body>" >> test.html
            echo "    <h1><a href=\"index.html\">Kata > $name</h1>" >> test.html
            echo "    $text" >> test.html
            echo "  </body>" >> test.html
            echo "</html>" >> test.html
          done
      - name: Add all changes
        run: git add --all
      - name: Status
        run: git status
      - name: Commit changes
        run: |
          git config --global user.name 'Your Name'
          git config --global user.email 'your-username@users.noreply.github.com'
          git commit -am "Automated report"
          git push
